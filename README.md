## Deployment Notes

This application requires access to a local webcam, operating system keyboard events,
and an active PowerPoint window. Due to these dependencies, it is intended to be run
locally and is not suitable for deployment on cloud platforms such as Render or Heroku.
